// create folder "server" on the same level as folder "src"
> npm init -y
> clear
> npm install express --save
// create file "server/index.js" and write code here
// Call to start nodejs server:
> node index

//++++++++++++++++++++++++++++++++++++++++++++
// install the packages in the package.json file:
> npm install

//++++++++++++++++++++++++++++++++++++++++++++
// Call GET and POST by Postman:
+ Call GET with only Url: http://localhost:3000

+ Call POST with :
    - Url: http://localhost:3000/signin
    - Headers: Content-Type: application/json
    - Body: Select JSON and input: {"name": "Vinh"}

